syntax = 'proto3';

package packagelogs;

import "google/protobuf/empty.proto";

message Log {
    string topic = 1;
    int32 level = 2;
    string msg = 3;
}

service LogService {
    rpc LogInfo(Log) returns (Log) {};
}